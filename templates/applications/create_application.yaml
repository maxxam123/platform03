apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: 
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: default
    name: ''
    server: 'https://kubernetes.default.svc' 
  project: default
  source:
    path: secret/
    repoURL: 'https:/github.com/maxxam123/platform01/applications/NAME01.git'
    targetRevision: main
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
